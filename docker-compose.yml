

services:
  bot:
    build: .
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    # Keep container running for development/debugging if needed
    command: tail -f /dev/null
